# Streetball Menu

Next.js + Tailwind CSS game start menu.

## Run

```bash
npm install
npm run dev
```

The menu uses `public/mee.png` as the full-screen background.

Routes:

- `/continue` uses `public/meesit.jpeg`
- `/new-game` uses `public/gang.jpeg`
# mee
